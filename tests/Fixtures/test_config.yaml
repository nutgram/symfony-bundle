framework:
  secret: "Three can keep a secret, if two of them are dead."
  test: ~

nutgram:
  token: '%env(string:TELEGRAM_TOKEN)%'
  safeMode: false
  routes: true
  config:
    botId: 123
    apiUrl: 'BlaBla'
    botName: 'EEE'
    testEnv: true
    pollingTimeout: 123
    pollingLimit: 456
    enableHttp2: true